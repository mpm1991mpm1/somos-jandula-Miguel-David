# Server Configuration
server.port=8094

# Application Name
spring.application.name=Reaktor-Redes

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/redes_db
spring.datasource.username=root
spring.datasource.password=toor
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Logging
logging.level.root=INFO
logging.level.com.redes.redes=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

app.security.profesor.username=profesor
app.security.profesor.password=profesor123
